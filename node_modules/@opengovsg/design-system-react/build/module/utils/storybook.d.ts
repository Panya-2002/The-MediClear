import { BoxProps } from '@chakra-ui/react';
import { Args, Decorator } from '@storybook/react';
export declare const fixedHeightDecorator: (height: BoxProps['h']) => Decorator;
/**
 * Viewports mapping viewport key to their width in (pixel) number.
 * Used for Chromatic viewpoint snapshots which requires the numbers in pixels.
 */
export declare const viewports: {
    sm: number;
    md: number;
    lg: number;
    xl: number;
};
export declare const getMobileViewParameters: () => {
    viewport: {
        defaultViewport: string;
    };
    chromatic: {
        viewports: number[];
    };
};
export declare const getTabletViewParameters: () => {
    viewport: {
        defaultViewport: string;
    };
    chromatic: {
        viewports: number[];
    };
};
export declare const mockDateDecorator: Decorator<Args>;
