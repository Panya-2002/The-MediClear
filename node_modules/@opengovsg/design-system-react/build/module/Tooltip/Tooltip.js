import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from 'react';
import { Box, Tooltip as ChakraTooltip, VisuallyHidden, } from '@chakra-ui/react';
export const Tooltip = ({ children, wrapperStyles, ...props }) => {
    // ChakraTooltip does not work on mobile by design. (see
    // https://github.com/chakra-ui/chakra-ui/issues/2691)
    // Hence adapt the tooltip to open when clicked on mobile
    const [isLabelOpen, setIsLabelOpen] = useState(!!props.isOpen);
    return (_jsxs(_Fragment, { children: [_jsx(ChakraTooltip, { ...props, hasArrow: true, isOpen: isLabelOpen, children: _jsx(Box, { as: "span", onMouseEnter: () => setIsLabelOpen(true), onMouseLeave: () => setIsLabelOpen(false), onClick: () => setIsLabelOpen((currentState) => !currentState), verticalAlign: "middle", sx: wrapperStyles, children: children }) }), _jsx(VisuallyHidden, { children: props.label })] }));
};
