import { type IconProps, type SystemStyleObject } from '@chakra-ui/react';
export interface ToggleChevronProps extends IconProps {
    reduceMotion?: boolean;
    isOpen?: boolean;
    styles: SystemStyleObject;
}
/**
 * ToggleChevron that gives a visual cue of the open/close state of the item.
 * It rotates `180deg` based on the open/close state.
 */
export declare const ToggleChevron: import("@chakra-ui/system/dist/system.types").ComponentWithAs<"div", ToggleChevronProps>;
