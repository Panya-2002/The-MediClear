import { jsx as _jsx } from "react/jsx-runtime";
import { Calendar } from '../../Calendar';
import { useDatePicker } from '../DatePickerContext';
export const DatePickerCalendar = () => {
    const { colorScheme, internalValue, isDateUnavailable, handleDateChange, initialFocusRef, monthsToDisplay, size, isMobile, defaultFocusedDate, } = useDatePicker();
    const displayedSize = isMobile ? 'sm' : size;
    return (_jsx(Calendar, { size: displayedSize, monthsToDisplay: monthsToDisplay, colorScheme: colorScheme, value: internalValue ?? undefined, isDateUnavailable: isDateUnavailable, onChange: handleDateChange, ref: initialFocusRef, defaultFocusedDate: defaultFocusedDate }));
};
