import { jsx as _jsx } from "react/jsx-runtime";
import { Fragment, useMemo } from 'react';
import { DarkMode, Tabs as ChakraTabs, useColorMode, } from '@chakra-ui/react';
export const Tabs = ({ colorMode: colorModeProp, ...tabsProps }) => {
    const { colorMode } = useColorMode();
    const colorModeToUse = colorModeProp ?? colorMode;
    const ColorModeWrapper = useMemo(() => {
        if (colorModeToUse === 'dark') {
            return DarkMode;
        }
        return Fragment;
    }, [colorModeToUse]);
    return (_jsx(ColorModeWrapper, { children: _jsx(ChakraTabs, { ...tabsProps }) }));
};
