/// <reference types="react" />
import { StyleFunctionProps, TabsProps as ChakraTabsProps } from '@chakra-ui/react';
export interface TabsProps extends ChakraTabsProps {
    /**
     * Whether to render the footer in dark or light mode.
     */
    colorMode?: StyleFunctionProps['colorMode'];
}
export declare const Tabs: ({ colorMode: colorModeProp, ...tabsProps }: TabsProps) => JSX.Element;
