import { jsx as _jsx } from "react/jsx-runtime";
import { Box, Icon, useMultiStyleConfig } from '@chakra-ui/react';
import { dataAttr } from '@chakra-ui/utils';
import { BxCheckAnimated } from '../../../icons';
export const ItemCheckboxIcon = ({ isChecked, isDisabled, size, }) => {
    const styles = useMultiStyleConfig('Checkbox', { size });
    return (_jsx(Box, { display: "inline-flex", alignItems: "center", justifyContent: "center", verticalAlign: "top", userSelect: "none", flexShrink: 0, bg: "white", __css: styles.control, "data-checked": dataAttr(isChecked), "aria-disabled": isDisabled, borderColor: "base.content.strong", children: _jsx(Icon, { as: BxCheckAnimated, __css: styles.icon, isChecked: isChecked }) }));
};
