import { FC, PropsWithChildren } from 'react';
import { ThemingProps } from '@chakra-ui/react';
export interface ToolbarContextProps {
    colorScheme?: 'main' | 'neutral' | 'sub';
    size?: ThemingProps<'Toolbar'>['size'];
}
export type ToolbarContextReturn = Required<ToolbarContextProps>;
export declare const ToolbarProvider: FC<PropsWithChildren<ToolbarContextProps>>;
export declare const useToolbarContext: () => Required<ToolbarContextProps>;
