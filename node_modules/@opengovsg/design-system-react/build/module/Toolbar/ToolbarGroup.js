import { jsx as _jsx } from "react/jsx-runtime";
import { Wrap } from '@chakra-ui/react';
import { useToolbarStyles } from './Toolbar';
export const ToolbarGroup = (props) => {
    const styles = useToolbarStyles();
    return (_jsx(Wrap, { shouldWrapChildren: true, overflow: "initial", flexDir: "row", __css: styles.group, spacing: "0.5rem", ...props }));
};
