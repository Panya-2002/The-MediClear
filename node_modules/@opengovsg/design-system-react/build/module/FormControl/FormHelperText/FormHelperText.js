import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo } from 'react';
import { FormHelperText as ChakraFormHelperText, Icon, useMultiStyleConfig, } from '@chakra-ui/react';
import { merge } from 'lodash';
import { BxsCheckCircle } from '../../icons';
/**
 * @precondition This element should be instantiated as a child of ChakraUI's `FormControl` element.
 *
 * An assistive component that conveys additional guidance about the field, such
 * as how it will be used and what types in values should be provided.
 */
export const FormHelperText = ({ children, ...props }) => {
    const styles = useMultiStyleConfig('Form', props);
    const mergedStyles = useMemo(() => merge({}, styles.helperText, props.sx), [props.sx, styles.helperText]);
    return (_jsxs(ChakraFormHelperText, { ...props, sx: mergedStyles, children: [props.variant === 'success' && (_jsx(Icon, { "aria-hidden": true, as: BxsCheckCircle, sx: styles.icon })), children] }));
};
