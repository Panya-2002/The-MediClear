import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo } from 'react';
import { Box, Flex, Icon, useMultiStyleConfig, } from '@chakra-ui/react';
import { BxsErrorCircle, BxsInfoCircle } from '../icons';
export const Infobox = ({ variant = 'info', children, icon: iconProp, size, ...flexProps }) => {
    const styles = useMultiStyleConfig('Infobox', { variant, size });
    const iconToRender = useMemo(() => {
        if (iconProp) {
            return _jsx(Box, { __css: styles.icon, children: iconProp });
        }
        return (_jsx(Icon, { as: variant !== 'error' ? BxsInfoCircle : BxsErrorCircle, __css: styles.icon }));
    }, [iconProp, styles.icon, variant]);
    return (_jsxs(Flex, { sx: styles.messagebox, ...flexProps, children: [iconToRender, children] }));
};
