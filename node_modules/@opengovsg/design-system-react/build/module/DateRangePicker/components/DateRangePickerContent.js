import { jsx as _jsx } from "react/jsx-runtime";
import { DatePickerContentBase } from '../../DatePicker/components/DatePickerContentBase';
import { useDateRangePicker } from '../DateRangePickerContext';
export const DateRangePickerContent = ({ children, }) => {
    const { isMobile, disclosureProps: { isOpen, onClose }, initialFocusRef, styles, size, } = useDateRangePicker();
    return (_jsx(DatePickerContentBase, { size: size, isMobile: isMobile, isOpen: isOpen, onClose: onClose, initialFocusRef: initialFocusRef, headerStyles: styles.header, children: children }));
};
