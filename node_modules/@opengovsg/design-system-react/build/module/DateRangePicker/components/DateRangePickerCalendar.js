import { jsx as _jsx } from "react/jsx-runtime";
import { RangeCalendar } from '../../Calendar';
import { useDateRangePicker } from '../DateRangePickerContext';
export const DateRangePickerCalendar = () => {
    const { colorScheme, internalValue, isDateUnavailable, handleCalendarDateChange, initialFocusRef, monthsToDisplay, size, isMobile, } = useDateRangePicker();
    const displayedSize = isMobile ? 'sm' : size;
    return (_jsx(RangeCalendar, { size: displayedSize, monthsToDisplay: monthsToDisplay, colorScheme: colorScheme, value: internalValue ?? undefined, isDateUnavailable: isDateUnavailable, onChange: handleCalendarDateChange, ref: initialFocusRef }));
};
