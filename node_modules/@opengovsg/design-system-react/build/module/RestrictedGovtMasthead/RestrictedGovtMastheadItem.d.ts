/// <reference types="react" />
import { As } from '@chakra-ui/react';
export interface RestrictedGovtMastheadItemProps {
    icon: As;
    header: string;
    children: React.ReactNode;
}
export declare const RestrictedGovtMastheadItem: ({ icon, header, children, }: RestrictedGovtMastheadItemProps) => JSX.Element;
