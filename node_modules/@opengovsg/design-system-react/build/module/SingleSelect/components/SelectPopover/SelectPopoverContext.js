import { createContext, useContext } from 'react';
export const SelectPopoverContext = createContext(undefined);
export const useSelectPopover = () => {
    const context = useContext(SelectPopoverContext);
    if (context === undefined) {
        throw new Error(`useSelectPopoverContext must be used within a SelectPopoverContextProvider`);
    }
    return context;
};
