/// <reference types="react" />
import { AlertStatus, UseToastOptions } from '@chakra-ui/react';
export type ToastStatus = AlertStatus;
export interface ToastProps extends Omit<UseToastOptions, 'duration' | 'position' | 'render' | 'variant'> {
    /**
     * RenderProps that chakra passes to all custom components that uses the
     * render function
     */
    onClose?: () => void;
}
export declare const Toast: ({ status, title, id, description, isClosable, onClose, onCloseComplete, ...toastStyleProps }: ToastProps) => JSX.Element;
