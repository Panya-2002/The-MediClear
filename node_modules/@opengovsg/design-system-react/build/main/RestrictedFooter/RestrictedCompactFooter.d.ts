import { FC } from 'react';
import { RestrictedFooterContainerProps, RestrictedFooterVariantProps } from './common/types';
interface RestrictedCompactFooterProps extends RestrictedFooterVariantProps {
    /** Logo to display on the compact footer. Will use @param appName if not provided. */
    brandLogo?: React.ReactNode;
}
/** Desktop only compact footer variant */
export declare const RestrictedCompactFooter: {
    ({ appName, footerIconLink, footerLinks, colorMode, brandLogo, appLink, containerProps, }: RestrictedCompactFooterProps): JSX.Element;
    Container: FC<RestrictedFooterContainerProps>;
};
export {};
