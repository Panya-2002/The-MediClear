/// <reference types="react" />
import { ComboboxItem } from '../../types';
export interface DropdownItemProps {
    item: ComboboxItem;
    index: number;
}
export declare const DropdownItem: ({ item, index, }: DropdownItemProps) => JSX.Element;
