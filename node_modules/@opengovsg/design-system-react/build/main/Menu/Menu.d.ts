import { FC } from 'react';
import { MenuProps as ChakraMenuProps } from '@chakra-ui/react';
import { ButtonProps } from '../Button';
import { MenuVariant } from '../theme/components/Menu';
export interface MenuButtonProps extends Omit<ButtonProps, 'isFullWidth'> {
    variant?: MenuVariant;
    isStretch?: boolean;
    isOpen?: boolean;
    chevronSize?: string;
}
interface MenuProps extends ChakraMenuProps {
    /** If true, menu list will match width of menu. Alias of `matchWidth=true` */
    isStretch?: boolean;
}
/**
 * Used to wrap MenuButton, MenuItem and MenuList components
 */
export declare const Menu: {
    ({ isStretch, ...props }: MenuProps): JSX.Element;
    Button: FC<MenuButtonProps>;
    List: import("@chakra-ui/system/dist/system.types").ComponentWithAs<"div", import("@chakra-ui/menu/dist/menu-list").MenuListProps>;
    Item: import("@chakra-ui/system/dist/system.types").ComponentWithAs<"button", import("@chakra-ui/menu/dist/menu-item").MenuItemProps>;
    Divider: FC<import("@chakra-ui/menu/dist/menu-divider").MenuDividerProps>;
};
export {};
