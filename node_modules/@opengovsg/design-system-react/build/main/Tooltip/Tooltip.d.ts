/// <reference types="react" />
import { SystemStyleObject, TooltipProps as ChakraTooltipProps } from '@chakra-ui/react';
export interface TooltipProps extends ChakraTooltipProps {
    /**
     * Text which appears on hover/focus.
     */
    label: string;
    /**
     * Styles for the container which wraps the children.
     */
    wrapperStyles?: SystemStyleObject;
}
export declare const Tooltip: ({ children, wrapperStyles, ...props }: TooltipProps) => JSX.Element;
